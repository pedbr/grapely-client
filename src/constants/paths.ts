const wineries = '/wineries'
const winery = '/winery/:wineryId'

const containers = '/containers'
const container = '/container/:containerId'

const batches = '/batches'
const batch = '/batch/:batchId'

const dashboards = '/dashboards'
const taskManager = '/task-manager'
const auth = '/'
const logout = '/logout'

export {
  auth,
  wineries,
  winery,
  containers,
  container,
  batches,
  batch,
  dashboards,
  taskManager,
  logout,
}
